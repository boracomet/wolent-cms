{
  "name": "wolent-cms-monorepo",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm --filter @wolent/core dev & pnpm --filter @wolent/admin dev",
    "build": "pnpm --filter @wolent/utils build && pnpm --filter @wolent/database build && pnpm --filter @wolent/core build && pnpm --filter @wolent/admin build && pnpm --filter @wolent/cli build",
    "cli": "node packages/cli/dist/index.js",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "setup": "pnpm --filter @wolent/core generate-keys && pnpm --filter @wolent/database db:push && pnpm --filter @wolent/core setup",
    "generate-keys": "pnpm --filter @wolent/core generate-keys"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "^3.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "pnpm": {
    "overrides": {
      "vite": "6.3.5"
    }
  }
}
